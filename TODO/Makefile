* Variable assignment should not expand variables, only recursively do so later at var expansion

* Paths relative/absolute/etc?

* Subdir-recursive pattern rules?

* Enumerate pattern outputs (wildcards, container dir of patterns)
